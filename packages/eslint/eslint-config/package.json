{
  "name": "@reliquary/eslint-config",
  "version": "1.0.0",
  "description": "Shareable, framework-agnostic ESLint 9 flat config — TypeScript, stylistic and import-hygiene rules. Add @reliquary/eslint-config-react for React projects.",
  "type": "module",
  "license": "MIT",
  "author": "Alex de Mulder",
  "keywords": [
    "eslint",
    "eslintconfig",
    "eslint-config",
    "flat-config",
    "typescript",
    "react"
  ],
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": "./index.js"
  },
  "main": "./index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "lint": "eslint index.js",
    "check:exports": "publint",
    "release:npm": "bun -e \"if(!process.env.RELIQUARY_RELEASE){console.error('Release only via the root scripts (bun run release:all or release:NAME), not directly');process.exit(1)}\" && bun publish && git tag -a \"$npm_package_name@$npm_package_version\" -m \"$npm_package_name@$npm_package_version\""
  },
  "peerDependencies": {
    "eslint": "^9.0.0"
  },
  "dependencies": {
    "@stylistic/eslint-plugin": "^2.0.0",
    "eslint-plugin-align-assignments": "^1.1.2",
    "eslint-plugin-align-import": "^1.0.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-import-newlines": "^1.3.4",
    "eslint-plugin-unused-imports": "^3.1.0",
    "globals": "^15.0.0",
    "typescript-eslint": "^8.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "publint": "^0.3.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
